#include<iostream>
#include <unistd.h>
#include <string.h>
#define argl 1024
#define cmdl 10

using namespace std;

int main(){
	int i;
	while(1){
		i=0;	
		char *st[argl];
		char *cmd;
		char nc;
		int rflag = 0;

		do{
			cmd = new char[cmdl];
		    cin>>cmd;
			st[i]=cmd;
			//cout<<st[i]<<endl;
			
			if(!strcmp(st[i],">>")) rflag = 1;
			else if(!strcmp(st[i],">")) rflag = 2;

			if(getchar() == '\n') break;
			i++;

		}while(1);

		st[++i]=NULL;	

	if(fork()){
        cout<<"hello from parent "<<endl;
    }
    else{
        //char *ed[] = {NULL};
		//cout<<"child "<<chk<<endl;
		if(rflag == 1){
			cout<<""<<endl;	
		}
		else if(rflag == 2){
			cout<<""<<endl;
		}
		else{
        		execvp(st[0],st);
		}
    }
	delete cmd;
	}

}
